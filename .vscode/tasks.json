{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "command": "sh",
    "args": [
        "-c", "cmake . && make && ctest --output-on-failure && valgrind --leak-check=yes --track-origins=yes bin/xionTest dataset && valgrind --leak-check=yes bin/xionTest preprocessing && echo 'build and test done'"
        ],
    "echoCommand": true,
    "tasks": [
        {
            "label": "sh",
            "type": "shell",
            "command": "sh",
            "args": [
                "-c",
                "cmake . && make && ctest --output-on-failure && valgrind --leak-check=yes --track-origins=yes bin/xionTest dataset && valgrind --leak-check=yes bin/xionTest preprocessing && echo 'build and test done'"
            ],
            "problemMatcher": [],
            "group": {
                "_id": "build",
                "isDefault": false
            }
        }
    ]
}